name: Build Web (HTML5)

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

jobs:
  web:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "HTML5 export wires up in M2 when we have a playable scene worth showing."
